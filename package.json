{
  "name": "fiber-village-3d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:zip": "node --experimental-strip-types scripts/check-zip.mjs",
    "check:geodesy": "node --experimental-strip-types scripts/check-geodesy.mjs",
    "check:network": "node --experimental-strip-types scripts/check-network.mjs"
  },
  "dependencies": {
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@zip.js/zip.js": "^2.8.34",
    "leaflet": "^1.9.4",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "@types/three": "^0.185.0",
    "typescript": "^6.0.2",
    "vite": "^8.1.1"
  }
}
